from dotenv import load_dotenv
import asyncio
import websockets
import json
import time

load_dotenv()

# Websocket Server

async def main():
    print("main")
    
if __name__ == "__main__":
    asyncio.run(main())